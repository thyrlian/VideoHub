:javascript
  $(window).load(function() {
    setVideoPlayerSize();
  });
  $(window).on('resize', function() {
    setVideoPlayerSize();
  });
  function setVideoPlayerSize() {
    var scale = 0.618;
    $('#video').attr({
      'width' : window.innerWidth * scale,
      'height' : 'auto'
    });
  };

%div{ :id => 'container' }
  %h1= @video.name
  %video{ :id => 'video', :class => 'centered', :controls => true }
    %source{ :src => @video.path.gsub(/^.*\/public/, ''), :type => "video/#{@video.type}" }
  %div{ :id => 'adjacency' }
    - if @idx < @videos.length - 1
      %div{ :style => 'float: right; margin-bottom: 8px' }
        %a{ :href => "#{@idx + 1}", :rel => 'next', :class => 'btn btn-default glyphicon chevron-right' }= @videos[@idx + 1].name
    - if @idx > 0
      %div{ :style => 'float: left; margin-bottom: 8px' }
        %a{ :href => "#{@idx - 1}", :rel => 'prev', :class => 'link-btn btn btn-default glyphicon chevron-left' }= @videos[@idx - 1].name
    %div{ :style => 'clear: both;' }
